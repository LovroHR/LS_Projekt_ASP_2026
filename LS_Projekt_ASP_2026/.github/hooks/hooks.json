{
  "hooks": {
    "note": "Logging is handled manually via PowerShell commands in terminal - see copilot-instructions.md",
    "UserPromptSubmit": [
      {
        "type": "manual",
        "instruction": "Run in terminal: $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss') + ' | UserPrompt: [prompt text]' | Add-Content -Path '.github/hooks/agent_log.txt'"
      }
    ]
  }
}